<%- include('partials/head.ejs'); -%>
  <% if(locals.user != undefined){ %>
      <%- include('/partials/headerLogueado') %>
  <%} else { %>
      <%- include('partials/header') -%>
  <% } %>
<section>
    <div class="container products-wrapper">
        <div class="d-flex align-items-center">
            <p class="product-detail-description">
                <a href="/product/add"><%= nombre %></a>
            </p>
        </div>

        <!-- Descripción del producto -->
        <div class="product-detail">
            <div class="row">
                <article class="col-12 col-lg-8 product-detail-info">
                    <img src="<%= fotoproducto %>" alt="<%= nombre %>" class="product-detail-img">

                    <p class="product-detail-description"><%= descripcion %></p>
                </article>

                <!-- Comentarios del producto -->
                <div class="col-12 col-lg-4">
                    <section class="comments">
                        <% if (comentarios && comentarios.length > 0) { %>
                            <% comentarios.forEach(comentario => { %>
                                <article class="comment">
                                    <img src="<%= comentario.fotoPerfil %>" alt="Usuario" class="comment-avatar">
                                    <div class="comment-data">
                                        <p class="comment-user">
                                            <strong>
                                                <a href="/profile"><%= comentario.usuario %></a>
                                            </strong>
                                            <br>
                                            <%= comentario.mensaje %>
                                        </p>
                                    </div>
                                </article>
                            <% }) %>
                        <% } else { %>
                            <p>No hay comentarios todavía. ¡Sé el primero en comentar!</p>
                        <% } %>
                    </section>
                    
                    <form class="comment-form" action="/product/<%= id %>" method="post">

                        <input class="form-input" type="text" name="comment" placeholder="Agregar comentario" required>
                        
                        <button type="submit" class="add-comment-button">AGREGAR COMENTARIO</button>
                    </form>
                    <hr>
                </div>
            </div>
        </div>
    </div>
</section>
<%- include('partials/footer.ejs'); -%>